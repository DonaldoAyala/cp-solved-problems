#include <bits/stdc++.h>

using namespace std;

int l, r, n;
vector<int> v;

int bsl(int b) {
	int a = 0, b = n - 1, m = a + (b - a)/2;


}

void solve() {
	cin >> n >> l >> r;
	v.resize(n);
	for(auto &x : v) cin >> x;
	sort(v.begin(), v.end());
	
}

int main () {
	int t; cin >> t;
	while(t--) solve();
	return 0;
}
