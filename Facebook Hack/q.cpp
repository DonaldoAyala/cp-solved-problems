#include <bits/stdc++.h>
#define lli long long int
#define pb push_back
#define fi first
#define se second

using namespace std;



int main () {
    int w, h, n; cin >> w >> h >> n;
    vector<vector<char>> v(h, vector<char> (w));
    for ( int i = 0 ; i < h ; i++ ) {
        for ( int j = 0 ; j < w ; j++ ) {
            
        }
    }   
    return 0;
}